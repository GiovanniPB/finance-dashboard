# Copie para `.dev.vars` (git-ignored) para rodar o Worker contra o stack LOCAL.
# Em `wrangler dev`, estes valores têm precedência sobre os `vars` do wrangler.jsonc,
# que apontam para produção.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=<a ANON_KEY que `supabase status` imprime>
